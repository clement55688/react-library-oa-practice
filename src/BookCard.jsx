export default function BookCard({ book }) {
  // TODO: title (h4), author, optional year inside article.book.
  return (
    <article className="book">
      <h4>Book title</h4>
    </article>
  );
}
